version: 2

models:
  - name: dim_CaasppAceComparisons
    description: "Publicly available CAASPP results data for ACE schools and select comparison schools and districts, and Santa Clara County"
    config:
      materialized: table
    columns:
      - name: "CountyCode || '-' || DistrictCode || '-' || SchoolCode || '-' || TestYear || '-' || DemographicId || '-' || GradeLevel || '-' || TestId"
        tests:
          - unique