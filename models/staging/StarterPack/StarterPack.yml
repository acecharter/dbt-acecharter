version: 2

models:
  - name: stg_SP__StudentAttendance_v2
    description: Staging model for StudentAttendance_v2 source data.
    columns:
      - name: AverageDailyAttendance
        tests:
          - is_less_than_1
  
  - name: stg_SP__StudentDemographics
    description: Staging model for StudentDemographics source data
    columns:
      - name: EllStatus
        tests:
          - not_null